spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/spendwise
    username: spendwise_user
    password: spendwise

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
